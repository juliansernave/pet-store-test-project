# Target API base URL (no trailing slash)
API_URL=https://petstore.swagger.io/v2

# API key sent as the api_key header on every request
API_KEY=special-key

# Optional: label shown in console output (dev | staging | prod)
TEST_ENV=dev
